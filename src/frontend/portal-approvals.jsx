import React,{useEffect,useState}from'react';import ForgeReconciler,{Button,Heading,Inline,Lozenge,Spinner,Stack,Text,TextArea}from'@forge/react';import{invoke}from'@forge/bridge';const PortalApprovals=()=>{const[items,setItems]=useState(null),[reasons,setReasons]=useState({}),[busy,setBusy]=useState(''),[error,setError]=useState('');const refresh=()=>invoke('getMyApprovals',{}).then(setItems).catch(e=>{setError(e.message||String(e));setItems([]);});useEffect(()=>{refresh();},[]);const decide=async(approvalId,decision)=>{setBusy(approvalId);setError('');try{await invoke('decideApproval',{approvalId,decision,reason:reasons[approvalId]||''});await refresh();}catch(e){setError(e.message||String(e));}finally{setBusy('');}};if(items===null)return<Spinner/>;const pending=items.filter(x=>x.status==='pending'),history=items.filter(x=>x.status!=='pending');return<Stack space="space.300"><Heading size="large">My Approvals</Heading><Text>Review requests assigned to you. Your decision is recorded against the Jira ticket.</Text>{error?<Text>{error}</Text>:null}<Heading size="medium">Awaiting my approval ({pending.length})</Heading>{pending.length===0?<Text>Nothing is waiting for you.</Text>:pending.map(a=><Stack key={a.id} space="space.100"><Inline space="space.100"><Heading size="small">{a.issueKey}: {a.summary}</Heading><Lozenge appearance="inprogress">Waiting</Lozenge></Inline>{a.message?<Text>Message: {a.message}</Text>:null}<TextArea value={reasons[a.id]||''} onChange={e=>setReasons({...reasons,[a.id]:e.target.value})} placeholder="Decision comment / decline reason"/><Inline space="space.100"><Button appearance="primary" onClick={()=>decide(a.id,'approved')} isDisabled={busy===a.id}>Approve</Button><Button appearance="danger" onClick={()=>decide(a.id,'declined')} isDisabled={busy===a.id}>Decline</Button></Inline></Stack>)}<Heading size="medium">Approval history</Heading>{history.length===0?<Text>No previous decisions yet.</Text>:history.slice(0,50).map(a=><Stack key={a.id}><Inline space="space.100"><Text><Text weight="bold">{a.issueKey}</Text> — {a.summary}</Text><Lozenge appearance={a.status==='approved'?'success':a.status==='declined'?'removed':'default'}>{a.status}</Lozenge></Inline></Stack>)}</Stack>};ForgeReconciler.render(<PortalApprovals/>);
